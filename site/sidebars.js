// Part groupings come entirely from site/docs/'s folder structure (regenerated by
// scripts/sync-docs.mjs from ../chapters/), so the sidebar needs no manual chapter list.

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  bookSidebar: [{type: 'autogenerated', dirName: '.'}],
};

module.exports = sidebars;
